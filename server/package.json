{
  "name": "server",
  "version": "1.0.0",
  "description": "server for \"double-click to translate extension\"",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^3.27.0"
  }
}
